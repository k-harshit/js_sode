const name='harshit';
 const repo_count=45;

 
// console.log(name  + " repo_count " + repo_count);
// above way of displaying info isn't recommended anymore instead use below depicted way
console.log(`my name is ${name} and my repo count value is ${repo_count}`);
const gameName = new String('harshit')
console.log(gameName);
console.log(gameName.__proto__); 
/* there are various functions inbuilt in string [refer MDN]*/
/**
 * charAt
 * indexOf
 * substring
 * slice
 * trim
 * etc
 * to search keywords (includes())
 * 
 * /
